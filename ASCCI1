#include <stdio.h>

int main() {
    // Variable to hold the character
    char character;
    
    // Input the character from the system
    scanf("%c", &character);
    
    // Print the ASCII value of the character
    printf("%d\n", (int)character);
    
    return 0;
}
